apiVersion: v1
kind: Template
metadata:
  name: gateway-service-template
objects:
- apiVersion: v1
  kind: BuildConfig
  metadata:
    labels:
      app: gateway-service
    name: gateway-service
  spec:
    output:
      to:
        kind: ImageStreamTag
        name: gateway-service:latest
    source:
      contextDir: Gateway
      git:
        uri: https://github.com/RHsyseng/FIS2-MSA.git
      type: Git
    strategy:
      sourceStrategy:
        from:
          kind: ImageStreamTag
          name: fis-java-openshift:2.0
          namespace: openshift
      type: Source
    triggers:
    - github:
        secret: MxjIZoWLfY9ySgCEc5mo
      type: GitHub
    - generic:
        secret: 2Qs5dlAL3FQGSlp5huoa
      type: Generic
    - type: ConfigChange
    - imageChange: {}
      type: ImageChange
- apiVersion: v1
  kind: ImageStream
  metadata:
    labels:
      app: gateway-service
    name: gateway-service
  spec:
    tags:
    - annotations: null
      from:
        kind: DockerImage
        name: ${DOCKER_HOST}:${DOCKER_PORT}/${PROJECT_NAME}/gateway-service:latest
      name: latest
- apiVersion: v1
  kind: DeploymentConfig
  metadata:
    labels:
      app: gateway-service
    name: gateway-service
  spec:
    replicas: 1
    selector:
      app: gateway-service
      deploymentconfig: gateway-service
    template:
      metadata:
        labels:
          app: gateway-service
          deploymentconfig: gateway-service
      spec:
        containers:
        - env:
          - name: ACTIVEMQ_BROKER_PASSWORD
            value: password
          - name: ACTIVEMQ_BROKER_USERNAME
            value: mquser
          - name: ACTIVEMQ_SERVICE_HOST
            value: broker-amq-tcp
          - name: ACTIVEMQ_SERVICE_PORT
            value: "61616"
          imagePullPolicy: IfNotPresent
          name: gateway-service
          ports:
          - containerPort: 8778
            name: jolokia
            protocol: TCP
          - containerPort: 9091
            protocol: TCP
    triggers:
    - type: ConfigChange
    - imageChangeParams:
        automatic: true
        containerNames:
        - gateway-service
        from:
          kind: ImageStreamTag
          name: gateway-service:latest
          namespace: ${PROJECT_NAME}
      type: ImageChange
- apiVersion: v1
  kind: Service
  metadata:
    labels:
      app: gateway-service
    name: gateway-service
  spec:
    ports:
    - name: jolokia
      port: 8778
      protocol: TCP
      targetPort: 8778
    - name: 9091-tcp
      protocol: TCP
      port: 9091
      targetPort: 9091
    selector:
      app: gateway-service
      deploymentconfig: gateway-service
parameters:
  - name: DOCKER_HOST
    description: Docker Registry Host IP
    value: 172.30.206.185
  - name: DOCKER_PORT
    description: Docker Registry Host Port
    value: "5000"
  - name: PROJECT_NAME
    description: OpenShift Parent Project Name
    value: fis2-msa
